  @"
spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/taxi
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: trip_service
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest
"